import {Inter, Lusitana } from 'next/font/google' //here next isnt actually a dir, its like a mock dir

export const inter = Inter({ subsets: ['latin'] })
export const lusitana = Lusitana({
    subsets:['latin'],
     weight:"400"
    })